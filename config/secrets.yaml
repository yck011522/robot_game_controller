# Local credentials for LAN devices. Closed private Ethernet; these are
# trivial lab passwords, so this file is committed to the repo on purpose.
# Read by: external_media_coordinator (planned) for SSH/SCP pulls from the
# audio Raspberry Pis.

pi_ssh:
  username: "pi"        # SSH user on every audio Pi (rpi5-11..rpi5-16 / 192.168.0.11-16)
  password: "pi1234"    # shared lab password for the pi account
